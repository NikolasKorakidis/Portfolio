import React from "react"
import PropTypes from "prop-types"

const example = props => {
  return (
    <div>
      <h1>Check it out</h1>
    </div>
  )
}

example.propTypes = {}

export default example
